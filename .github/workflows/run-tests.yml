# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will download a prebuilt Ruby version, install dependencies and run tests with Rake
# For more information see: https://github.com/marketplace/actions/setup-ruby-jruby-and-truffleruby

name: Run tests with rake

on:
  push:
    branches: [ rubocop-211208 ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  test:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby-version: ['.ruby-version', '2.7']
    services:
      postgres:
        image: postgres:13
        ports:
          - "5432:5432"
        env:
          POSTGRES_DB: rails_test
          POSTGRES_USER: rails
          POSTGRES_PASSWORD: password
    env:
      RAILS_ENV: test
      DATABASE_URL: "postgres://rails:password@localhost:5432/rails_test"

    steps:
    - uses: actions/checkout@v3
    - name: Set up Ruby
    # It uses v1 as a code for "The latest version with automatic fixes etc.", referred to as a floating V1
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby-version }}
        bundler-cache: true # runs 'bundle install' and caches installed gems automatically
    - name: Setup custom host entry for www.nothing.fish
      run: echo "127.0.0.1 www.nothing.fish" | sudo tee -a /etc/hosts

    - name: Debug hosts
      run: cat /etc/hosts

    - name: Set up database schema
      run: bin/rails db:schema:load

    - name: Run tests
      run: bundle exec rake
      env:
        SERVER_PRODUCTION: www.nothing.fish
        SERVER_STAGING: www.nothing.fish
        SERVER_DEVELOPMENT: www.nothing.fish
        HOST_PORT: 80
        COMMITTEE: 12.5
        REQ_URL: https://www.nothing.fish/items/%s
        MAIL_SENDER: admin@nothingfish.org
